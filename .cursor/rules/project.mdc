---
description: General rules for the Legal Ai Ar project. Always apply.
globs:
alwaysApply: true
---

# Legal Ai Ar — Project Instructions

## Read first — current state

Before doing anything, read **[`docs/roadmap/STATUS.md`](../../docs/roadmap/STATUS.md)** — the single
source of truth for *where the project stands and what to pick up next*. When you finish a work item,
update STATUS.md (progress log + "Next up"). Application code lives at the repo root (`backend/`,
`frontend/`, `infra/`, `deployment/`).

## Identity

Legal Ai Ar is a Legal Knowledge Base system with AI Agents for the Argentine legal domain. It centralizes legislation, case law, and legal doctrine, combining hybrid search (BM25 + vectors) with specialized agents built on Semantic Kernel.

## Tech stack

- **Frontend**: Angular 19 (standalone components), Tailwind CSS 4, Cytoscape.js
- **Backend**: .NET 10, Clean Architecture (4 layers), Minimal API, EF Core 10
- **AI/RAG**: Azure OpenAI (GPT-5o, GPT-5o-mini), Semantic Kernel, text-embedding-3-large (3072d)
- **Data**: Azure SQL (relational + Graph Tables), Azure AI Search (hybrid search), Azure Blob Storage
- **Messaging**: Azure Storage Queues, Azure SignalR Service
- **Hosting**: Azure App Service
- **CI/CD**: GitHub Actions

## Monorepo structure

```
legal-ai-ar/
├── backend/
│   ├── src/ … api, shared (Core, Infrastructure, Agents), workers, tools
│   ├── tests/ … xUnit projects (+ LegalAiAr.AgentEvals)
│   ├── LegalAiAr.sln
│   ├── Directory.Packages.props
│   └── global.json
├── frontend/                     # Angular 19 SPA
├── infra/                        # Azure provisioning scripts
├── deployment/                   # GCaaS Helm chart
├── docs/
│   ├── standards/                            # PwC internal app architecture baseline
│   ├── roadmap/                              # Features, work items, backlog
│   ├── technical/                            # Product-specific technical docs
│   └── ontology/                             # Legal domain model
└── README.md
```

## Releases

| Release | Name | Status |
|---------|------|--------|
| R0.0 | Preparation | In progress |
| R1.0 | Research & Monitoring | Pending |
| R2.0 | Professional Productivity | Pending |
| R3.0 | Knowledge & Intelligence | Pending |
| R4.0 | Scale & Operations | Pending |

## Mandatory rules

1. **Do not touch code directly**: NEVER create, edit, or delete source code files unless the user explicitly requests it. Instead, indicate which file to create or modify, in which path, and provide the code for the user to place. Correct example: "Create the `LegalNormService.cs` class in `backend/src/api/LegalAiAr.Application/Services/` with the following code: ..."
2. **Language rule (strict)**:
   - **Everything in English**: code (variables, methods, classes, interfaces, enums), file names, components, tables, indexes, storage, repos, URLs, endpoints, DTOs, domain entities, commits, technical documentation, work items, code comments.
   - **Spanish only for**: end-user facing messages in the app (UI labels, error messages, tooltips, user-visible content).
   - **Two exceptions kept in Spanish**: the agents' YAML prompt content and the mockup labels.
   - **Dev chat**: can be in Spanish or English.
   - Example: class is `LegalNorm`, table is `LegalNorms`, endpoint is `/api/legal-norms`, but the UI label says "Normas Legales".
3. **Monorepo**: the project uses a single repository `legal-ai-ar`; never create separate repos.
4. **Naming consistency**: use "Legal Ai Ar" (display name) or "LegalAiAr" (.NET namespace). NEVER use "LegalKB".
5. **Agent projects**: `LegalAiAr.Agents` in `backend/src/shared/` and `LegalAiAr.AgentEvals` in `backend/tests/`.
6. **Infrastructure & delivery**: CI/CD and IaC are operated by the delivery track (documented + tracked, not application features). Two delivery paths: GitHub Actions → Azure staging (API + SPA) and GCaaS Helm/Knative → corporate production. See `docs/deployment/github-delivery.md`, `docs/deployment/gcaas-hosting.md`, features.md §2.3, feature FT05.
7. **Authentication**: production auth is platform-managed Entra SSO on GCaaS — the platform issues an `id_token` HTTP-only cookie; the API validates it (`Auth:Platform`). Do NOT use MSAL, app-owned `/login`, or Bearer tokens; the SPA uses `withCredentials`. See `docs/deployment/gcaas-hosting.md`.
8. **Definition of Done — documentation round-trip (mandatory)**: a work item is **NOT done** until its documentation is updated: the work-item file (Tasks/Acceptance `[x]`), `docs/roadmap/STATUS.md` (progress log + "Next up"), and any affected docs (`docs/standards/*`, `docs/technical/*`, `docs/ontology/*`, `docs/deployment/*`, `features.md`, onboarding) synced to the merged code. **NEVER mark a work item complete or approve/finish its PR while documentation is pending.** Applies to every dev + AI. See `docs/roadmap/DEFINITION-OF-DONE.md`.
9. **Architecture baseline**: follow [`docs/standards/pwc-internal-app-architecture.md`](../../docs/standards/pwc-internal-app-architecture.md) for auth, layering, API patterns, frontend structure, testing, and delivery. Product docs in `docs/technical/` extend it; they must not contradict it.

## Reference documentation

Before generating code or work items, consult:

- `docs/standards/pwc-internal-app-architecture.md` — **Architecture baseline** for internal PwC apps
- `docs/roadmap/features.md` — Full roadmap with features, endpoints, and KPIs
- `docs/roadmap/backlog.md` — Feature and work-item inventory
- `docs/technical/` — Technical decisions (RAG, agents, prompts, ingestion, eval, security, observability, UX, data)
- `docs/ontology/` — Argentine legal domain model
- `docs/deployment/github-delivery.md` + `docs/deployment/gcaas-hosting.md` — Delivery and hosting (GitHub CI/CD to Azure staging; GCaaS Knative/Helm + platform Entra auth)
