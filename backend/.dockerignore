# Build outputs (avoid overwriting container restore with host obj/bin)
**/bin/
**/obj/

# IDE and user files
**/.vs/
**/*.user
**/*.suo

# Test results
**/TestResults/
**/*.trx
**/*.coverage

# NuGet local cache (use container restore only)
**/.nuget/
