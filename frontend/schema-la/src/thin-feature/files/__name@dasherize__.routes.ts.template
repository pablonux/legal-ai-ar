import { Routes } from '@angular/router';

export const <%= camelize(name) %>Routes: Routes = [
  {
    path: '',
    loadComponent: () =>
      import('./pages/<%= dasherize(name) %>-page.component').then((m) => m.<%= classify(name) %>PageComponent)
  }
];
