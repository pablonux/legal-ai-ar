# F1.3 - W02 - Case-Law Search Backend Alignment

> **Feature:** F1.3 - Case-Law (Jurisprudence) Search
> **Release:** 1.0 | **Sprint:** S01
> **Type:** backend | **Priority:** High
> **Estimate:** 3 story points
> **Assignable to:** Backend Dev

---

## Description

Align the existing rulings search backend (~90% from MVP) to Contracts DTOs, complete the
Minimal API endpoint wiring from F0.0-W09, and ensure all filter/facet/pagination capabilities
are operational.

---

## Tasks

### Contracts

- [ ] **Create** `backend/src/shared/LegalAiAr.Contracts/Rulings/SearchRulingsRequest.cs` (per F1.3-W01 §3.2)
    - Note: `SearchRulingsRequestMapper.cs` and `SearchRulingsRequestValidator` exist from F0.0-W09/W10 — refactor to use new Contract type
- [ ] **Create** `backend/src/shared/LegalAiAr.Contracts/Rulings/RulingSummary.cs`
- [ ] **Create** `backend/src/shared/LegalAiAr.Contracts/Rulings/SearchRulingsResponse.cs`
- [ ] **Create** `backend/src/shared/LegalAiAr.Contracts/Rulings/RulingDetailResponse.cs` (minimal — full detail in F1.6)

### Application layer

- [ ] **Verify / complete** `SearchRulingsQuery` + handler in `LegalAiAr.Application`
    - Calls `ICaseLawSearchService.SearchAsync`; maps to `SearchRulingsResponse`
    - Filters: `Court`, `CourtVenue`, `Instance`, `IsEnBanc`, `TopicKeywords`, date range, sort, page
- [ ] **Verify / complete** `GetRulingFacetsQuery` + handler
- [ ] **Create** `GetRelatedRulingsQuery` + handler
    - Executes `CitesCaseLaw` graph traversal (1 hop); returns `IReadOnlyList<RulingSummary>`

### Endpoints (update stubs from F0.0-W09)

- [ ] **Verify / update** `PostRulingsSearch.cs` — bind `SearchRulingsRequest`; return `SearchRulingsResponse`
- [ ] **Verify / update** `GetRulingsFacets.cs` — wire to `GetRulingFacetsQuery`
- [ ] **Verify / update** `GetRulingRelated.cs` — wire to `GetRelatedRulingsQuery`
- [ ] **Verify / update** `GetRulingById.cs` — return `RulingDetailResponse` (minimal)
- [ ] **Verify / update** `GetRulingDocument.cs` — stream from Blob or `OriginalUrl` redirect

### Search service (Infrastructure)

- [ ] **Verify / update** `CaseLawSearchService.cs`
    - Build `SearchOptions` for all ruling filter fields
    - Map `topicKeywords` filter as `Collection(Edm.String)` filter expression
    - Ensure highlights on `headnote` and `fullText`

### Tests

- [ ] Unit: `SearchRulingsQueryHandler` — court + instance filter maps to correct options
- [ ] Unit: `GetRelatedRulingsQueryHandler` — returns related ruling IDs from graph
- [ ] Integration: `POST /api/rulings/search` with `{ "court": "CSJN" }` → 200 filtered results
- [ ] Integration: `GET /api/rulings/{id}/related` → list of related ruling summaries

### Verification

- [ ] `dotnet build` zero warnings; `dotnet test` green
- [ ] `POST /api/rulings/search` with `isEnBanc: true` returns only en-banc rulings
- [ ] `22-api-endpoints.md` updated; DoD round-trip

---

## Acceptance Criteria

- [ ] All ruling search filters functional; highlights present in results
- [ ] Related rulings endpoint returns `CitesCaseLaw` graph traversal results
- [ ] All Contracts DTOs used at API boundary
- [ ] Zero warnings; tests green

---

## Dependencies

- **Prerequisites:** F1.1-W02 (auth), F0.0-W09 (stubs), F0.0-W10 (Contracts)
- **Provides:** F1.3-W03 (frontend)

---

_F1.3 - W02 - Case-Law Search Backend Alignment — Legal Ai Ar_
